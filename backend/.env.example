PORT=4000
# Cadena de conexion a MongoDB (Reemplaza con tus credenciales)
MONGO_URI=mongodb+srv://<usuario>:<password>@cluster.mongodb.net/baches?retryWrites=true&w=majority
# Clave secreta para firma de tokens JWT
JWT_SECRET=tu_clave_secreta_jwt_aqui
